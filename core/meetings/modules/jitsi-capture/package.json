{
  "name": "@vexa/jitsi-capture",
  "version": "0.1.0",
  "description": "Jitsi Meet capture (browser): dominant-speaker watcher over the app's own redux state (→ mixed-capture.v1 hints, with a DOM fallback) + chat reader/sender over the same state (chat panel need not be open). Mixed audio from @vexa/mixed-capture-core; names resolve downstream.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "test": "tsx src/jitsi-capture.test.ts",
    "check:isolation": "node scripts/check-isolation.js"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^26.4.1",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2"
  }
}
